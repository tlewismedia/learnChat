<h1>New Course section</h1>

<%= render "form.html", Map.put(assigns, :action, Routes.course_section_path(@conn, :create)) %>

<span><%= link "Back", to: Routes.course_section_path(@conn, :index) %></span>
